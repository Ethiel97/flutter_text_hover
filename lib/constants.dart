import 'package:flutter/material.dart';

class Constants {
  static const duration = Duration(milliseconds: 200);
  static const letterWidth = 12.0;
  static const textStyle = TextStyle(
    fontWeight: FontWeight.normal,
    color: Colors.black,
    fontSize: 18,
  );
}
